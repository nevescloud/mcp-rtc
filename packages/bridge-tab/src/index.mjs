// @jonasneves/mcp-rtc-bridge-tab — browser-side library that re-exposes a
// remote mcp-rtc server's tools as WebMCP tools in the local tab.
//
// Status: scaffolded. Implementation lands after @jonasneves/mcp-rtc's
// browser transport stabilizes.

export {};
